Algoritmo "TABUADA ESTRUTURA REPETITIVA"

Var

 n, i, produto:inteiro

Inicio

  escreva("deseja tabuada para qual valor ? ")
  leia(n)
  
  para i de 1 ate 10 faca

     produto <- n * i
     escreval(n, "X", i, " = ", produto)

  fimpara

Fimalgoritmo