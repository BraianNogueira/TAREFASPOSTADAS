Algoritmo "tstcodigosmatematicos"

Var

  x, y, Z, a, b, c, d:real

Inicio

  x <- 5.0
  y <- 4.0
  Z <- -9.0

  a <- exp(x,y)
  b <- raizQ(y)
  c <- pi
  d <- Abs(Z)
  
  
  escreval(a)
  escreval(b)
  escreval(c)
  escreval(d)

Fimalgoritmo