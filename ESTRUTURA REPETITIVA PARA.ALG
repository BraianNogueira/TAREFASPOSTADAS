Algoritmo "estrutura repetitiva para"

Var

 soma, i, n, x:inteiro

inicio
  escreva("quantos numeros seram digitados: ")
  leia(n)
  
  soma <- 0
  
  
  para i de 1 ate n faca

    escreval("digite um numero: ")
    leia(x)
    soma <- soma + x
  
  fimpara

  escreval("soma = ", soma)
Fimalgoritmo